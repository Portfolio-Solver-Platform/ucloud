user nginx;
error_log  /var/log/nginx/error.log notice;


events {
    worker_connections 1024; # default value
}

# Headscale
http {
    # Basic settings
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # Logging
    access_log /var/log/nginx/access.log;

    server {
        listen 80;
        listen [::]:80;

        server_name clustervpn.jonasbork.dk;

        # Proxy /vpn requests to headscale
        location / {
            proxy_pass http://localhost:8080;
            proxy_http_version 1.1;

            # WebSocket support (needed for headscale)
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";

            # Forward headers
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Increase timeouts for long-polling
            proxy_read_timeout 300s;
            proxy_connect_timeout 75s;

            # Buffering settings
            proxy_buffering off;
            proxy_request_buffering off;
        }
    }
}

# stream {
# 	map $ssl_preread_server_name $proxy {
# 		default backend;
# 	}
#
# 	upstream backend {
# 		server control:30443;
# 		server worker0:30443;
# 		server worker1:30443;
# 	}
#
# 	server {
# 		listen 443;
# 		listen [::]:443;
# 		proxy_pass $proxy;
# 		ssl_preread on;
# 	}
# }
